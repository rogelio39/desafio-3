config:
  target: 'http://localhost:3000/api/operacionCompleja'
  phases:
    - duration: 60
      arrivalRate: 15

scenarios:
  - flow:
    - get:
        url: '/'
        headers:
          authorization: 'aqui copiamos un token de jwt'